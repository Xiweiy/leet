
SELECT DISTINCT NUM AS ConsecutiveNumbers
FROM (
    SELECT NUM, @COUNT := IF(@PREV =NUM, @COUNT +1, 1) AS TIMES, @PREV := NUM
    FROM Logs, (SELECT @COUNT :=0, @PREV:= NULL) AS I) AS TEMP
WHERE TEMP.TIMES >=3;

